/* Clase utilizada para los botones de las acciones en la tabla reutilizable (datatable.component). */
export interface TableButton {
    id: number;
    nombre: string;
    class: string;
    accion: any;
}